<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".view.VideoDetails_2">

    <!-- Widget for ExoPlayer view, centered on the screen -->
    <com.google.android.exoplayer2.ui.StyledPlayerView
        android:id="@+id/player_view"
        android:layout_width="624dp"
        android:layout_height="284dp"
        app:resize_mode="fit"
        app:use_controller="true"
        app:show_buffering="when_playing"
        app:show_shuffle_button="true">

        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone" />
    </com.google.android.exoplayer2.ui.StyledPlayerView>

    <!-- Fullscreen toggle button -->

    <!-- Horizontal list of movies at the bottom -->

    <!-- Vertical list of movies on the right side -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/horizontal_movie_list"
        android:layout_width="730dp"
        android:layout_height="123dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="1dp"
        android:layout_marginLeft="1dp"
        android:layout_marginBottom="0dp"
        android:orientation="horizontal"
        android:padding="8dp"
        tools:listitem="@layout/item_movie" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/vertical_movie_list"
        android:layout_width="115dp"
        android:layout_height="288dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_marginEnd="1dp"
        android:layout_marginRight="1dp"
        android:orientation="vertical"
        android:padding="8dp"
        tools:listitem="@layout/item_movie" />

</RelativeLayout>
